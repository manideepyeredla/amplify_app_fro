// src/components/Header.js
import { Amplify } from 'aws-amplify';
import React from 'react';

const Header = () => {
  return (
    <header>
      <h1>AWS Amplify Query App</h1>
    </header>
  );
};

export default Header;